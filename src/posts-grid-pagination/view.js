/**
 * Posts Grid Pagination — front-end view module.
 *
 * Imports the shared store so the `prevPage` / `nextPage` actions and the
 * `state.*` bindings used in render.php are available.
 */

import '../store';
