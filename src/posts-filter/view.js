/**
 * Posts Filter — front-end view module.
 *
 * Imports the shared store so `actions.toggleTerm` and the `state.isChecked`
 * binding used by the checkboxes resolve. The filter can therefore live
 * anywhere on the page and still update the same store the grid renders from.
 */

import '../store';
